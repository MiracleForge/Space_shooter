/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

if (part_type_exists(eff_system))
{
	part_type_destroy(eff_system);	
}
if (part_type_exists(eff_type))
{
	part_type_destroy(eff_type);	
}
if (part_emitter_exists(eff_system,emitter))
{
	part_emitter_destroy(eff_system,emitter);	
}
















