/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
	/*
instance_destroy(self);
effect_create_above(ef_explosion, x, y, 0.5, c_white);
with(Ometeor_parents){
	
		meteor_hp -= 1;
			 if meteor_hp <=0{	
					instance_destroy();	
					}

}